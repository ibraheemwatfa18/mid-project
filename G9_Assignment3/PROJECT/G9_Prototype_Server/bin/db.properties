# GoNature Database Configuration
# Edit these values to match your MySQL installation.
# The password is entered at runtime via the startup dialog and is NOT stored here.
db.url=jdbc:mysql://localhost/park_db?serverTimezone=UTC
db.user=root

# Gmail SMTP settings for outgoing notification emails.
# mail.password is a Gmail App Password (not the normal account password).
mail.sender=official.gonature@gmail.com
mail.password=elrucwuncdldoafc
mail.host=smtp.gmail.com
mail.port=465
